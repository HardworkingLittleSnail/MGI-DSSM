# Experiments 章节结构设计

> 本文档分为两个层次：第一层忠实记录本地相关文献的实验章节设计；第二层才根据本文模型的主张提出实验建议。标记为“本文专属”的实验并不表示其他文章普遍做过，而是为了支撑本文独有的物理闭环主张。

# A. 我们自己的 Experiments 最终应包含哪些小节

下面这套目录是本文实验章节的主结构。它综合了本地八篇相关论文、两篇扩展参考论文以及当前 `mgi-physics` 实现中的真实流程，不再把实验简单概括成“主实验—消融—效率”。完整章节建议由八个一级小节组成；每个小节内部的实验、表格和图件均有明确任务。

章节顺序不照搬模型的计算顺序，而遵循审稿人的问题顺序：先确认比较协议公平，再立即判断模型是否在单步和更困难的递归任务中有效；预测性能成立后，再审计反演状态与物理闭合，随后通过消融和理论分析解释性能来源；最后用扰动实验、计算成本和失败案例限定方法的可靠范围。这样主结果不会被反演细节推迟，物理分析也不会脱离已经观察到的性能事实。

| 我们的正式小节                     | 核心问题                                       | 主要产出                             |
| ---------------------------------- | ---------------------------------------------- | ------------------------------------ |
| 4.1 实验设置                       | 数据、任务、基线和统计协议是否公平可复现？     | 数据/协议表、基线表、退化概览图      |
| 4.2 跨电池单步容量预测             | 未见电池的下一循环容量能否准确预测？           | 三数据集主表、全部测试电池容量网格图 |
| 4.3 严格递归多步预测               | 不使用未来观测时长期递推是否稳定？             | 预测跨度表、递归轨迹图、EOL结果      |
| 4.4 观测—状态反演的有效性验证      | 五维状态是否确实由曲线稳定反演并闭合到容量？   | 反演表、$V(Q)$/DVA重建图、闭合图     |
| 4.5 消融实验与设计选择分析         | 性能与物理闭环分别来自哪些设计？               | 分组大消融、历史/损失/转移敏感性     |
| 4.6 物理一致性、可解释性与理论分析 | 状态如何演化、如何影响容量，约束具有什么性质？ | 状态轨迹、灵敏度、理论命题、可辨识性 |
| 4.7 鲁棒性与敏感性分析             | 测量噪声、缺测、先验失配和协议改变时是否可靠？ | 压力测试表、鲁棒性/敏感性图          |
| 4.8 计算效率、案例分析与实际可行性 | 完整链路成本与适用边界是什么？                 | 效率表、Pareto图、案例与失败分析     |

正文二级标题统一压缩如下；每个标题内部可包含若干实验、表格和分析段落，但不再继续拆成碎片化标题。

| 一级小节           | 二级小节安排                                                               |
| ------------------ | -------------------------------------------------------------------------- |
| 4.1 实验设置       | 数据集和预处理；预测任务与评测协议；超参数设置                             |
| 4.2 单步预测       | 与先进方法对比；逐电池退化轨迹与寿命阶段表现                               |
| 4.3 多步预测       | 预测跨度下的递归精度；不同寿命阶段的递推稳定性；寿命终点定位与RUL误差      |
| 4.4 反演验证       | 反演覆盖率与数值可靠性；观测曲线的物理重建；状态—容量闭合与可辨识边界      |
| 4.5 消融分析       | 反演约束的必要性；时序表示与状态转移机制；物理解码、训练目标与模块协同     |
| 4.6 物理与理论分析 | 状态演化与容量响应；受限转移的理论性质与可辨识边界；局部回升行为与状态机制 |
| 4.7 鲁棒性分析     | 测量扰动与输入退化；物理先验失配敏感性；超参数与评测协议敏感性             |
| 4.8 效率与可行性   | 端到端成本与精度—效率权衡；代表性表现与失败案例；部署假设与适用边界        |

## 4.1 实验设置

本节建立所有后续结果成立的评测基础，只划分为三个二级小节：先说明数据如何形成可用样本，再统一任务、基线和评价协议，最后给出实现与复现细节。

### 4.1.1 数据来源与样本构建

需要报告CALCE、NASA和TJU的电池型号、化学体系、额定容量、循环数量、充放电协议、温度、截止电压、放电电流、原始测量字段以及用于训练/验证/测试的电池。除数据表外，增加一张三数据集容量退化概览图，使读者在看到结果前理解三者在寿命长度、容量再生、噪声水平和退化速率上的差异。

**直接文献依据：**AttMoE Fig. 2首先比较CALCE与NASA容量退化噪声；PatchFormer Fig. 1将CALCE、NASA、TJU容量曲线并列；MSTEA-Net Fig. 1展示CALCE与TJU退化曲线；Nature Communications 2024 Fig. 2先展示不同工况下的退化轨迹，再进入预测实验。

**表4.1｜数据集与评测划分**

| 数据集 | 化学体系           | 额定容量 | 电池数 | 循环数 | 原始测量  | 放电协议 |      截止电压 | 训练/验证/测试电池 | 预测起点SP |
| ------ | ------------------ | -------: | -----: | -----: | --------- | -------- | ------------: | ------------------ | ---------: |
| CALCE  |                    |   1.1 Ah |        |        | $V,I,t,R$ |          |         2.7 V |                    |            |
| NASA   | LCO/graphite proxy |   2.0 Ah |      4 |        | $V,I,t$   | 2 A CC   | cell-specific | B0006/7/18 → B0005 |         50 |
| TJU    | NCM+NCA            |   2.5 Ah |      3 |        | $V,I,t$   | 2.5 A CC |         2.5 V | CY25-2/3 → CY25-1  |    200/400 |

说明如何识别放电阶段、划分循环、积分得到容量坐标、截取首次截止事件、将原始曲线与官方容量标签保序对齐，以及异常值和缺失值处理。必须单独给出因果性声明：主结果中的测试预处理不能使用目标之后的循环。当前双侧局部$3\sigma$插值作为“文献兼容预处理”单独报告，不能与无清洗或因果清洗结果混写。

增加一个数据保留流程表：原始循环数、成功解析循环数、有效放电循环数、成功对齐循环数、反演成功循环数和最终预测窗口数。这样既回应预处理是否改变样本的问题，也为反演失败率提供分母。

**直接文献依据：**PatchFormer把数据采集、异常值清理、划分、训练和可视化写成实验流程图；MSTEA-Net明确使用mean ± $3\sigma$删除异常点并插值；Nature Communications 2024在Discussion中专门讨论了充放电协议引起的信息泄漏，并采用跨电池训练/测试避免同一电池前后段泄漏。因此，本文不仅应给出预处理步骤，还应把因果性作为可审计实验协议。

### 4.1.2 预测任务与比较协议

分别定义：

1. 过去$L$个循环预测下一循环绝对容量的单步任务；
2. 从相同历史终点出发且不再读取未来观测的严格递归多步任务；
3. 以固定SP开始、持续预测至EOL的RUL/退化轨迹任务；
4. 同一数据集内未见电池测试与跨化学数据集复现实验。

正文必须区分“连续单步预测”“递归多步预测”“从SP到EOL的轨迹预测”和“跨数据集迁移”，避免把不同任务的数值放在同一表中直接排名。

将约12个对比方法分成五组：

1. 无学习基线：Persistence、线性趋势；
2. 统计/浅层模型：AR、SVR、GPR；
3. 经典深度时序模型：LSTM、GRU、TCN；
4. 近年强时序/电池模型：PatchFormer、RUL-Mamba、ModernTCN、iTransformer/TimeMixer以及能获得源码的近年电池模型；
5. 物理引导对照：可复现PINN/混合模型和本文的physics-loss-only变体。

每个基线记录输入、源码、发表年份、参数选择方式、是否重训练、是否使用测试电池前缀和是否与本文使用相同SP。原文引用结果与本地同协议复现结果必须分开。

**直接文献依据：**BatteryMFormer将基线分为电池专用模型和通用时序模型；MSTEA-Net先比较经典模型、再比较先进模型；BATTER-MoE同时比较经典、Transformer和电池专用模型。这三种分层方式比把所有模型堆成单一列表更清楚。

容量预测报告MAE、RMSE和$R^2$；EOL/RUL报告AE与RE，但只有真实与预测轨迹均达到阈值时才定义RE。每个配置至少运行10次，报告mean ± std；另给出逐电池结果和跨电池宏平均。对高度重叠的滑动窗口，不把每个窗口当作独立统计样本，置信区间和显著性分析应以电池或独立训练运行作为统计单位。

增加三类诊断指标：相对Persistence改进率、早/中/晚寿命分段MAE、最坏电池或P95循环误差。最佳单种子结果可以放补充材料，但不能代替主表中的均值和方差。

**直接文献依据：**BATTER-MoE、Nature Communications 2024和PatchFormer均采用重复运行；BATTER-MoE Fig. 4与Nature Communications 2024 Fig. 4/5均直接显示均值和标准差，而不是只给一次最优结果。

### 4.1.3 实现细节与复现设置

报告硬件、软件、epoch、batch size、隐藏维度、GRU层数、学习率、优化器、早停、容量网格、OCP profile、$\tau_p$、损失权重及随机种子。只保留影响复现的关键配置；完整搜索空间放补充材料。超参数只由训练/验证电池确定，测试电池不能用于选择配置或随机种子。

**直接文献依据：**PatchFormer使用TPE/Optuna并公开搜索范围；BATTER-MoE给出逐数据集架构和训练参数并进一步分析Top-$k$；BattNN和PINN-PHM在附录系统改变层数、神经元数、序列长度及候选物理输入。因此，我们至少应公开选择空间、验证准则和最终配置，而不只列最终数值。

---

## 4.2 跨电池单步容量预测

这是第一项主实验，回答模型在未见电池上的下一循环绝对容量预测能力。

### 4.2.1 总体定量对比

**目的：**证明模型在统一协议下优于简单基线和强基线，并且结果可重复。

**表结构：**行是约12个模型；列按CALCE、NASA和TJU分组，每组报告MAE、RMSE和$R^2$的10次运行mean ± std；增加Persistence、线性趋势、参数量和平均排名。逐电池完整数值放补充表，正文避免过宽。

### 4.2.2 逐电池退化轨迹与寿命阶段表现

**保留，且只保留这一张容量轨迹主图。**它要证明的是：模型是否在所有正式测试电池上持续跟踪退化趋势、局部容量回升和晚寿命变化，而不是只获得一个较低平均误差。

**图结构：**定量网格图。若正式测试对象为CALCE四块、NASA B0005和TJU CY25-1，则设置六个电池面板；CALCE四个小面板位于第一行，NASA和TJU各占第二行一半。每个电池面板由容量主轴和下方窄误差轴组成：

- 容量主轴：SP之前真值用浅灰色，SP之后真值用黑色，本文10次平均预测用主色，阴影为$\pm1$标准差；竖线标SP，横线标EOL；
- 误差轴：绘制$\Delta e_k=|\widehat Q_k-Q_k|-|Q_{k-1}-Q_k|$，零线以下表示优于Persistence；
- 每个面板只标MAE和RMSE，$R^2$、AE/RE保留在表中。

**不叠加十余个基线，也不再另画三数据集代表性综合图。**模型排名由表4.2完成；重复综合图会浪费版面。完整基线轨迹和逐随机种子曲线放补充材料。

**能够证明：**全寿命跟踪、局部回升响应、晚寿命漂移、跨种子稳定性，以及相对Persistence的逐循环收益。

**不能证明：**总体优于所有基线或差异显著；这两个结论必须由表4.2和统计结果支持。

该设计保留MSTEA-Net Fig. 3–4“逐电池真实—预测—误差”的核心逻辑，并增加重复运行不确定性和Persistence对照。

早、中、晚寿命及局部容量回升区间的MAE放入一张紧凑补充表，不再增加主图。

### 4.2.3 异构数据集上的架构适用性

CALCE、NASA和TJU使用不同采集协议与化学先验，因此不应被描述为共享一套数据预处理参数或同一组模型权重。本文在三个数据集上分别训练模型，但固定五维代理状态定义、曲线反演—状态转移—物理解码主体流程、时序输入组织和训练目标，仅依据数据集已知条件配置放电电流、截止电压、容量网格及OCP profile。表4.2应同时报告三个数据集的结果，表4.4则报告其曲线重建与closure，从而验证同一主体架构能否适用于异构数据来源。

这一证据只支持“跨数据集架构适用性”或“同一架构在多数据集上的复现”，不支持zero-shot transfer、跨域权重泛化或统一宏观输入。若后续确需提出权重迁移主张，必须另做source-only、few-shot和full-training对照。

**表4.2｜留一电池协议下的单步预测结果**

| 模型         | 类型           | 输入             | 参数量 | CALCE MAE/RMSE/$R^2$ | NASA MAE/RMSE/$R^2$ | TJU MAE/RMSE/$R^2$ | 平均排名 |
| ------------ | -------------- | ---------------- | -----: | -------------------- | ------------------- | ------------------ | -------: |
| Persistence  | naive          | last capacity    |      0 |                      |                     |                    |          |
| Linear trend | statistical    | capacity history |      0 |                      |                     |                    |          |
| ...          |                |                  |        |                      |                     |                    |          |
| MGI-DSSM     | physics-guided | inverted states  |        |                      |                     |                    |          |

---

## 4.3 严格递归多步预测

这是第二项主实验，验证状态转移是否能在未来曲线不可见时稳定递推。

### 4.3.1 预测跨度下的递归精度

从同一历史终点开始，对所有模型执行严格递归预测，报告$H=4,8,16,32$（数据允许时增加64）的MAE/RMSE/$R^2$。第1步之后不得读取真实容量、真实曲线或真实反演状态。另报告误差增长斜率和状态越界率，以区分短期准确与长期稳定。

### 4.3.2 不同寿命阶段的递推稳定性

分别从早、中、晚寿命起点进行rollout，展示真实容量、模型预测、Persistence、线性外推和最强深度基线。若晚寿命误差增长明显，应在结果中直接呈现，而不是只展示最好的起点。

### 4.3.3 寿命终点定位与RUL误差

对能够跨过EOL阈值的轨迹报告真实EOL、预测EOL、AE和RE；未跨阈值时报告右删失而不是填入0或任意数值。NASA B0005若测试区间没有真实阈值穿越，则RE为NaN是协议结果，不应通过修改公式强行生成数值。

**文献关系：**PatchFormer、BATTER-MoE和MSTEA-Net均改变SP并预测至EOL，但它们没有完整验证本文这种“五维状态不经未来校正的严格递归rollout”。因此，不同SP与EOL图形形式可借鉴文献，而严格状态递归本身属于本文必须新增的贡献验证。

**表4.3｜不同预测跨度下的严格递归预测结果**

| 模型 | $H=4$ MAE | $H=8$ MAE | $H=16$ MAE | $H=32$ MAE | 误差增长斜率 | 状态越界率 | EOL AE/RE |
| ---- | --------: | --------: | ---------: | ---------: | -----------: | ---------: | --------: |
|      |           |           |            |            |              |            |           |

---

## 4.4 观测—状态反演的有效性验证

前两项主实验已经回答模型“能否准确预测”。本节进一步验证预测所依赖的五维状态是否确实由观测曲线稳定获得，并能通过同一物理解码器闭合到容量，从而回答性能结果是否具有可信的物理中间过程。

### 4.4.1 反演覆盖率与数值可靠性

报告每个数据集的曲线解析率、对齐误差、反演成功率、多初值优化失败率、参数边界命中率和单循环反演时间。选取早、中、晚寿命三个循环展示原始$V(Q)$、用于反演的采样点和截止位置。

### 4.4.2 观测曲线的物理重建

展示真实与重建$V(Q)$、残差以及$dV/dQ$对齐，并按早、中、晚寿命统计电压MAE/RMSE和DVA误差。不能只展示容量闭合，因为不同参数组合可能产生相近截止容量，却不能解释完整曲线形状。

**文献借鉴：**BattNN Fig. 5/6先展示真实电流—电压样本，BatteryMFormer Fig. 6把模型关注区间与DVA峰/肩部对应。本文应把这两类呈现改造成“观测曲线—物理重建—DVA残差”的直接验证，而不是使用注意力热图。

### 4.4.3 状态—容量闭合与可辨识边界

把每个反演状态重新输入同一个物理截止求解器：

$$
\widehat Q_k^{\mathrm{cl}}=G_{\mathrm{phys}}(Z_k),
$$

报告closure MAE、RMSE、P95、随循环变化曲线和超过阈值的比例。当前代码已经在状态转移训练前执行closure门槛检查，因此该结果应被正式写入论文，而不是只作为运行日志。

对同一曲线使用不同初值、轻微采样扰动和噪声重复反演，比较状态方差、重建误差及容量闭合误差；同时给出五维状态相关矩阵。若不同状态组合仍能得到相近曲线，应将其解释为有效整电池代理状态，而不是唯一材料参数。

**表4.4｜状态反演与物理闭合结果**

| 数据集/电池 | 解析曲线数 | 对齐MAE | 反演成功率 | 电压MAE | DVA误差 | 闭合MAE | 闭合P95 | 边界命中率 | 单循环耗时 |
| ----------- | ---------: | ------: | ---------: | ------: | ------: | ------: | ------: | ---------: | ---------: |
|             |            |         |            |         |         |         |         |            |            |

---

## 4.5 消融实验与设计选择分析

本节采用“一张总消融表＋若干针对性分析图”，并将相关设计合并为三个实验组，而不是为每个开关单独设置标题。

### 4.5.1 反演约束的必要性

比较：去除$dV/dQ$残差、去除截止点强调、去除内阻弱观测、去除相邻循环连续性、单初值替代多初值、普通最小二乘替代soft-$L_1$。同时报告电压MAE、closure MAE、状态方差、边界命中率和最终容量MAE。

### 4.5.2 时序表示与状态转移机制

比较：仅状态水平、状态＋一阶差分、增加8步趋势、增加32步趋势、完整20维表示；并设置GRU→MLP、单尺度GRU和不同历史长度。AttMoE Fig. 3通过改变隐藏维度分析MoE贡献，BattNN Fig. 14和DNNwMF系统改变历史长度，BatteryMFormer Fig. 4改变可用早期循环数。这些均说明时间信息的价值应通过连续设计量而非单一删除来验证。

比较：乘性绝对状态更新、加性更新、无界直接状态预测；最后状态头零初始化与随机初始化；允许局部回调与强单调方向损失。重点报告单步误差、多步误差、状态越界率及局部容量恢复片段表现。

### 4.5.3 物理解码、训练目标与模块协同

比较：完整物理截止容量解码、学习型直接容量头、$Q_k+\Delta Q$残差头、capacity-only模型及Persistence。这是证明“预测状态必须通过物理解码产生容量”而非装饰性物理模块的核心消融。

分别去除curve loss、weak state loss、capacity loss、late-life weighting和regeneration weighting；方向损失从0到非零作为单独设计分析。PINN-PHM比较简单损失相加与自适应平衡并展示五轮权重轨迹；BattNN比较weighted MAE与MSE；MSTEA-Net直接删除PINN约束。因此本文也应把每个训练目标与它声称改善的指标对应起来。

对相互依赖的关键组件增加二阶消融，例如“无曲线监督＋无物理解码”“无多尺度＋无受限转移”。BATTER-MoE的Synergistic Effects of Core Components不是只逐个删除模块，还检查组件组合；本文同样需要确认增益是否由单个强组件独立产生。

**表4.5｜完整模型的分组消融结果**

| 分组           | 变体                   | 单步MAE | 16/32步MAE | 曲线MAE | 闭合MAE | 状态越界率 | 参数量 |
| -------------- | ---------------------- | ------: | ---------: | ------: | ------: | ---------: | -----: |
| Full           | MGI-DSSM               |         |            |         |         |            |        |
| Inversion      | w/o DVA                |         |            |         |         |            |        |
| Representation | w/o multi-scale trends |         |            |         |         |            |        |
| Transition     | unconstrained additive |         |            |         |         |            |        |
| Decoder        | learned capacity head  |         |            |         |       — |            |        |
| Objective      | w/o curve loss         |         |            |         |         |            |        |

---

## 4.6 物理一致性、可解释性与理论分析

这一节对应原计划中的“理论分析”，将状态观察、理论性质与典型机制组织为三个相互衔接的分析单元，而不是为每一种诊断量单列标题。

### 4.6.1 状态演化与容量响应

展示$Q_{\mathrm{Li}}$、$C_n$、$C_p$、$R_0$和$R_p$随循环变化，并与真实容量、预测容量及EOL位置对齐。报告跨种子均值/方差、边界命中率和变化率分布，分别分析长期趋势、平台、局部回升和加速退化阶段。

计算

$$
S_i(k)=\frac{\partial G_{\mathrm{phys}}(Z_k)}{\partial Z_{k,i}},
$$

并用中心有限差分复核自动微分。比较早、中、晚寿命阶段以及不同电池，说明各状态何时影响截止容量。BatteryMFormer Fig. 6通过DVA峰与注意区域对应来解释模型；本文更适合直接解释“状态扰动如何改变电压曲线与截止容量”。

### 4.6.2 受限转移的理论性质与可辨识边界

对方法中能够严格推导的性质给出命题或附录证明：乘性更新保持状态为正；$\tanh$限制单步相对变化；累积最小和首次阈值穿越保证容量求解采用第一次截止；容量网格加密时线性插值误差收敛。然后用数值实验检验这些性质是否在实际预测中转化为更低越界率和更稳定rollout。解析性质不能替代预测结果，但能够解释模型为何不会产生某些非物理解。

报告反演Jacobian奇异值、多初值结果、状态相关矩阵和profile perturbation。该分析的目标是识别哪些状态被曲线充分约束、哪些只能作为组合状态；不强行证明五个状态均唯一可辨识。完整结果建议放补充材料，正文保留最关键的奇异值谱或相关矩阵。

### 4.6.3 局部回升行为与状态机制

预先定义容量回升或平台片段，对比允许局部回调、强单调转移和无约束转移，展示容量及五状态轨迹。BatteryMFormer用三种退化形态做代表案例，PatchFormer强调容量再生点；本文的案例分析应直接回答为什么方向损失设为0以及“reversible”在观测层面的含义。

**表4.6｜物理一致性与状态行为汇总**

| 数据集 | 状态正值率 | 边界命中率 | 闭合MAE | 曲线MAE | DVA误差 | 多初值变异系数 | 局部回升MAE |
| ------ | ---------: | ---------: | ------: | ------: | ------: | -------------: | ----------: |
|        |            |            |         |         |         |                |             |

---

## 4.7 鲁棒性与敏感性分析

本节同时覆盖输入扰动、物理先验失配和关键设计量，建议区分三组。

### 4.7.1 测量扰动与输入退化

对测试时电压加入与设备精度相符的噪声及其2倍/4倍强度，随机丢失5%/10%/20%采样点，执行2×/4×/8×降采样，并对电流加入偏置。每种扰动分别报告反演状态变化、curve/closure误差、单步误差和多步误差，以定位错误发生在反演还是转移阶段。

AttMoE Fig. 4把高斯噪声施加到不同比例的原始数据；BATTER-MoE Table X使用混合burst/Gaussian训练噪声。本文不能只在容量标签上加噪声，因为模型的核心输入是放电曲线，扰动应落在$V/I/t$测量层。

### 4.7.2 物理先验失配敏感性

改变OCP profile、$\tau_p$、放电电流和截止电压，比较预测与closure变化。设置“匹配化学先验”“错误化学先验”和“弱化物理先验”三组，用于判断模型的跨数据集表现究竟来自结构共享还是精细先验配置。

### 4.7.3 超参数与评测协议敏感性

系统改变历史长度$L$、短/长趋势窗口、隐藏维度、容量网格点数、损失权重和SP。每个因素选择3–5个值，其他配置固定。主文只保留历史长度、网格密度和最关键损失权重三项；完整热图放补充材料。

**直接文献依据：**BattNN改变序列长度和训练样本数；DNNwMF从$n=0$逐步增加至$n=17$并同时报告误差与参数量；BATTER-MoE把Top-$k$与误差/参数量联合分析；PatchFormer公开TPE搜索。由此，本节应同时展示性能趋势和计算代价，不能只给“最优值”。

**表4.7｜观测扰动与先验失配条件下的鲁棒性**

| 扰动类型        |            强度 | 反演成功率 | 状态偏差 | 闭合MAE | 单步MAE | 32步MAE |
| --------------- | --------------: | ---------: | -------: | ------: | ------: | ------: |
| Voltage noise   |              1× |            |          |         |         |         |
| Missing samples |             10% |            |          |         |         |         |
| OCP mismatch    | wrong chemistry |            |          |         |         |         |

---

## 4.8 计算效率、案例分析与实际可行性

### 4.8.1 端到端成本与精度—效率权衡

分别测量神经状态转移器参数量、FLOPs、训练时间、首次曲线反演时间、状态缓存生成时间、缓存大小、单步状态预测、物理容量求解、严格多步rollout和峰值内存。CPU与GPU、batch size=1和批量推理分别报告中位数与P95。

**直接文献依据：**MSSA-SNN设置独立的resource consumption小节；AttMoE Fig. 5比较两个数据集上的时间；BATTER-MoE同时报告参数、FLOPs、CPU/GPU latency、显存和训练时间；BattNN报告参数、每epoch时间和总训练时间；MSTEA-Net在模型比较表中加入100 epochs耗时并单列feasibility analysis。因此本文必须报告完整链路，而不能只给GRU参数量。

绘制MAE—在线延迟和MAE—参数量Pareto图，并将“冷启动含反演”与“状态已缓存的在线运行”分开。若反演耗时较大，准确表述为“紧凑在线状态转移器＋离线状态反演”，不能笼统声称整个系统始终比纯数据模型快。

### 4.8.2 代表性表现与失败案例

代表案例应覆盖典型退化、明显容量回升、EOL附近快速下降和最坏误差四类。每个案例同时展示原始曲线、重建曲线、五状态、容量预测、Persistence与最强基线，说明错误来自观测反演、状态转移还是截止解码。BatteryMFormer Fig. 5对超线性、线性和亚线性退化进行案例分析；本文应进一步加入最坏案例，避免只选最好看的电池。

### 4.8.3 部署假设与适用边界

列出BMS在线需要获取的测量、状态反演是否离线执行、测试时是否需要完整放电、不同化学体系需要哪些先验、异常循环如何处理以及无法到达EOL阈值时RE如何报告。MSTEA-Net的Feasibility analysis和MSSA-SNN的资源分析均把实验结果落到部署条件；本文也应以明确假设结束实验章节。

**表4.8｜端到端效率与部署成本**

| 模型 | 参数量 | FLOPs | 训练时间 | 单循环反演时间 | 缓存大小 | 状态转移延迟 | 解码延迟 | 在线总延迟 | 峰值内存 |  MAE |
| ---- | -----: | ----: | -------: | -------------: | -------: | -----------: | -------: | ---------: | -------: | ---: |
|      |        |       |          |                |          |              |          |            |          |      |

---

## A.1 我们实验章节的完整证据顺序

最终的阅读节奏应当是：

$$
\underbrace{\text{协议与数据可信}}_{4.1}
\rightarrow
\underbrace{\text{单步预测有效}}_{4.2}
\rightarrow
\underbrace{\text{严格多步仍稳定}}_{4.3}
\rightarrow
\underbrace{\text{状态确实来自观测且能够闭合}}_{4.4}
\rightarrow
\underbrace{\text{增益可归因于具体设计}}_{4.5}
\rightarrow
\underbrace{\text{物理行为与理论性质可审计}}_{4.6}
\rightarrow
\underbrace{\text{扰动下边界清楚}}_{4.7}
\rightarrow
\underbrace{\text{代价和部署条件可接受}}_{4.8}.
$$

这八节形成“协议可信—效果成立—困难任务成立—内部过程可信—增益可归因—物理行为可解释—压力条件可承受—实际代价可接受”的递进关系。单步主结果必须紧跟实验设置，避免读者在尚不知道模型是否有效时先进入反演细节；反演与闭合随后承担解释主结果可信性的任务。跨数据集架构适用性由4.2与4.4的三数据集联合结果给出，不再独立设置“泛化”小节。

## A.2 文献实验设计与本文小节的逐项映射

| 参考论文                                  | 原文实际实验设计                                                                              | 原文主要表图                          | 本文吸收位置                                                           |
| ----------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------- |
| MSSA-SNN (IEEE TCE 2023)                  | Experimental Setup；Eunicell案例；Powerstream案例；Resource Consumption                       | 两数据集容量轨迹、$R^2$雷达图、资源表 | 4.1数据设置；4.2轨迹图；4.8资源与部署                                  |
| AttMoE (JES 2024)                         | Overall Performance；MoE/隐藏维度；dropout；5%–35%原始数据噪声；Time Cost                     | Fig. 2–5、Table 1–2                   | 4.2主结果；4.5模块消融；4.7噪声；4.8时间                               |
| PINN-PHM (2023)                           | SoH与RUL两任务；Case A/B/C；显式Verhulst/DeepHPM/纯数据比较；自适应损失；网络和候选输入敏感性 | Fig. 6–10、Table III–XI               | 4.5损失；4.6物理动力学；4.1超参数                                      |
| BATTER-MoE (2025/2026稿)                  | 三数据集Early/Late主比较；组件协同；Top-$k$；效率；混合噪声；有限目标数据迁移                 | Fig. 4–6、Table IV–XI                 | 4.2三数据集总图；4.5交互消融；4.7噪声；4.8效率；少数据设计移至补充实验 |
| BatteryMFormer (KDD 2026稿)               | 四领域主结果；可用早期循环数；组件消融；三类退化案例；DVA对应；50%训练数据                    | Table 2–4、Fig. 4–6                   | 4.2分阶段；4.5消融；4.6 DVA/案例；少数据设计移至补充实验               |
| BattNN (IEEE TNNLS 2023)                  | 三数据集EOD；损失对照；序列长度；训练样本数；参数和训练时间；Discussion                       | Fig. 11–15、Table III–V               | 4.4曲线呈现；4.5损失；4.7长度敏感性；4.8效率                           |
| PatchFormer (JPS 2025)                    | TPE超参数；CALCE I/II、NASA、TJU不同SP主实验；结构消融                                        | Fig. 4、6、7预测曲线，Fig. 8消融      | 4.1调参；4.2三数据集轨迹；4.3 SP/EOL；4.5结构消融                      |
| MSTEA-Net (Energy 2026)                   | 预测精度；STL/DWT/VMD；经典与先进模型；六组消融；效率；可行性                                 | Fig. 3–6、Table 4–8                   | 4.1基线分层；4.2逐电池曲线；4.5物理/结构消融；4.8可行性                |
| Wang et al., Nature Communications 2024   | 四数据集总体结果；16个特征相关性；10次稳定性；训练电池数量；跨数据集fine-tuning；泄漏讨论     | Fig. 3–6、Table 2–4                   | 4.1协议审计；4.2多数据集适用性；4.6解释边界；少数据/迁移移至补充实验   |
| DNN with memory features (Frontiers 2023) | NASA/CALCE；历史$n$从0到17；autoencoder与无压缩；参数—精度权衡；跨CALCE工况                   | Fig. 5–9、Table 2–6                   | 4.5历史表示；4.7长度敏感性；4.8精度—复杂度                             |

其中，Nature Communications 2024的实验设计可从[原文](https://www.nature.com/articles/s41467-024-48779-z)核对；DNN with memory features的实验结构可从[原文](https://www.frontiersin.org/journals/energy-research/articles/10.3389/fenrg.2023.1059701/full)核对。其余八篇均来自上一级“相关文献”目录中的本地PDF。

## A.3 主文图表总清单

### 建议主文表格

1. 表4.1：数据集、原始测量、化学体系、划分、SP与任务；
2. 表4.2：三数据集单步主结果；
3. 表4.3：不同horizon严格多步结果；
4. 表4.4：曲线解析、反演、重建与closure；
5. 表4.5：分组大消融；
6. 表4.6：物理一致性与状态行为；
7. 表4.7：噪声、缺测和先验失配；
8. 表4.8：完整链路效率与部署成本。

### 建议主文图件

1. 图4.1：三数据集容量退化特征与评测划分；
2. 图4.2：MSTEA-Net式逐测试电池真实容量—预测容量—逐循环误差图；
3. 图4.3：多步误差—预测跨度曲线与三类起点递归轨迹；
4. 图4.4：早/中/晚寿命$V(Q)$、$dV/dQ$和反演重建；
5. 图4.5：五状态轨迹、解码容量和局部灵敏度；
6. 图4.6：物理先验/噪声压力测试；
7. 图4.7：准确率—参数量/在线延迟Pareto图；
8. 图4.8：典型、容量回升、EOL和失败案例。

### 建议补充材料

- 正文报告全部正式测试电池的均值预测轨迹；补充材料提供每个随机种子的独立容量曲线和全部基线轨迹；
- 每个SP的完整结果；
- 全部约12个基线的配置和复现来源；
- 超参数搜索空间、验证结果和最优选择；
- 所有反演初值、边界命中和失败循环；
- 完整Jacobian/Fisher/profile结果；
- 全噪声、缺测、降采样和先验扰动强度；
- 可选的25%/50%/75%/100%训练循环块实验；只有在正文或摘要提出“小样本/数据效率”主张时才升级为主文；
- 可选的source-only、few-shot与full-training跨数据集迁移；只有完成该实验后才使用“权重迁移”或“zero-shot”表述；
- 逐模块参数量、FLOPs和延迟测量脚本；
- 代表图选择规则与原始结果索引。

## A.4 三位审稿人视角下仍必须守住的底线

### 审稿人1：预测协议与统计证据

- 主要关切：是否真正跨电池、是否超过Persistence、是否使用测试电池调参、10次运行是否报告均值而非最好值。
- 通过条件：4.1给出无泄漏协议，4.2同时报告简单与强基线，4.3明确严格递归，所有主结果以电池/独立运行统计。

### 审稿人2：物理状态与解释证据

- 主要关切：五维状态是否只是人为构造、物理解码是否真的决定容量、不同化学体系是否依赖专属调参。
- 通过条件：4.4完成曲线重建与closure，4.5包含物理解码替换消融，4.6报告状态灵敏度和可辨识边界，4.7给出OCP先验失配。

### 审稿人3：实际价值与复现性

- 主要关切：模型是否只在清洗后的公开小数据集有效，反演代价是否抵消轻量网络优势，部署是否需要现实中难获得的完整放电曲线。
- 通过条件：4.2与4.4在三数据集上共同给出预测结果和反演闭合，4.7从原始测量层做压力测试，4.8报告冷启动和在线完整成本并说明测量假设。少数据实验仅在论文提出数据效率主张时成为必需。

### 综合审稿结论

三类审稿风险最终汇聚为三个不能缺失的证据：严格无泄漏的跨电池主结果、观测—状态—容量的双重闭合、包含反演在内的端到端成本。只要其中任意一项缺失，即使单次MAE很好，仍不足以建立本文“准确、物理可审计且轻量”的完整论证。

---

# B. 文献原始实验结构与直接先例

本节保留逐篇阅读记录，用于核对A节中每项建议的来源。A节是本文最终采用的目录，本节是证据库。

## B.1 八篇本地文献逐篇实验结构

#### 文献1：A Brain-Inspired Spiking Network Framework Based on Multi-Time-Step Self-Attention

原文实验章节按以下顺序展开：

1. **Experimental Setup**：介绍数据、对比模型、训练配置和评价方式；
2. **Case 1: Eunicell Li-Ion Battery Dataset**：在第一个电池数据集上进行逐电池容量预测和五种方法比较；
3. **Case 2: Powerstream Li-Ion Battery Dataset**：在第二个来源的数据上重复主实验，用于检验跨数据来源表现；
4. **Comparative Analysis of Resource Consumption**：比较SNN与ANN类模型的计算资源消耗。

该文没有系统的大规模模块消融、严格多步rollout或可辨识性分析。它最值得参考的是“两个数据集案例＋独立资源分析”的紧凑结构。

#### 文献2：AttMoE

原文首先设置实验条件：

1. **3.1 Data sets**：CALCE和NASA；
2. **3.2 Baseline approaches**：MLP、LSTM、GRU和Dual-LSTM；
3. **3.3 Evaluation metrics**：RE、MAE、RMSE和leave-one-out；
4. **3.4 Parameter settings**：采样长度、学习率、注意力头数、隐藏维度和专家数。

随后Results and discussion包括：

1. **4.1 Overall performance**：主对比表；
2. **4.2 Effect of Mixture of Experts**：删除MoE，并随隐藏维度变化比较；
3. **4.3 Effect of dropout**：有/无dropout对照，并向5%–35%的原始数据加入高斯噪声；
4. **4.4 Time cost**：比较不同模型训练时间。

直接先例是“主实验＋核心模块消融＋噪声鲁棒性＋耗时”。该文没有物理状态闭合或物理参数解释实验。

#### 文献3：Physics-Informed Neural Networks for Prognostics and Health Management of Lithium-Ion Batteries

原文先介绍A123数据、特征提取、标准化、Case A/B/C划分、网络和DeepHPM输入组合。结果部分包括：

1. **SoH Estimation**：比较vanilla NN、GPR、显式Verhulst动力学PINN和DeepHPM PINN；
2. **RUL Prognostics**：在Case A/B/C上比较RUL误差；
3. **Adaptive balancing analysis**：比较直接相加多损失与不确定性自适应权重，并展示权重训练轨迹；
4. **附录敏感性**：比较隐藏层数、神经元数和DeepHPM候选输入组合；
5. **Discussion**：讨论不同工况下物理动力学模型的适用性和失配。

它提供的是“显式物理模型/发现型物理模型/纯数据模型”的层级比较，以及损失平衡和候选物理输入分析；没有本文这种五状态到截止容量的闭环实验。

#### 文献4：BATTER-MoE

Experimental Setup包括：

1. **Datasets and Preprocessing**；
2. **Problem Formulation and Evaluation Protocol**；
3. **Evaluation Metrics**；
4. **Baseline Models**；
5. **Training and Hyperparameters**。

结果章节依次为：

1. **Comparative Performance Analysis**：NASA、GOTION、TJU及早/晚SP主对比；原文Fig. 4将三个数据集放入同一张综合图，左侧比较MAE/RMSE及十次运行误差条，右侧展示对应的真实容量退化曲线与预测曲线，并按Early/Late设置组织；
2. **Synergistic Effects of Core Components**：大规模组件消融；
3. **Optimization of MoE Key Parameters**：Top-k与误差/参数量关系；
4. **Parameter Efficiency Under Resource-Constrained Settings**：参数量、FLOPs、CPU/GPU延迟、峰值内存和训练时间；
5. **Robustness to Noisy Training Inputs**：不同噪声比例；
6. **Transfer Learning with Limited Target Data**：有限目标数据迁移。

这是八篇中实验覆盖最完整的一篇，直接支持我们参考“主实验—消融—超参数—效率—噪声—少数据”的章节节奏。

其中Fig. 4是本文最值得直接参考的主结果图形结构：它不是为每个数据集各放一张孤立图片，而是通过统一坐标风格、统一颜色和统一Early/Late行布局，把三个数据集的误差统计与容量预测轨迹放在一张图中，使读者可以同时比较跨数据集准确性、预测稳定性和退化曲线拟合情况。

#### 文献5：BatteryMFormer

原文实验章节为：

1. **4.1 Experimental Settings**：四个电池领域、aging-condition-exclusive划分、MAE/MAPE、基线和实现；
2. **4.2 Overall Performance**：四领域主结果；
3. **4.2.2 Comparison under different numbers of usable cycles**：改变可用早期循环数量；
4. **4.3 Ablation Study**：删除SOCView、MDPM、ACDecoder、ACAttention、ACQuery和LLM嵌入；
5. **4.4 Case Study**：选择超线性、线性和亚线性退化案例，展示记忆原型和注意力；
6. **DVA correspondence**：把高注意力SOC区间与微分电压峰及肩部区域对应；
7. **4.5 Data-Efficient Learning**：只保留50%训练电池。

它直接提供“不同观测长度＋组件消融＋代表案例＋DVA解释＋少数据”的参考，但其任务是完整退化轨迹预测，不等于本文的递归状态rollout。

#### 文献6：Inherently Interpretable Physics-Informed Neural Network for Battery Modeling and Prognosis

原文先介绍Lab、NASA和Simulated三套数据以及实验细节，结果章节为：

1. **EOD Prediction Results**：CNN、LSTM与BattNN三数据集比较，五次实验均值和误差条；
2. **Effectiveness of the Proposed Loss**：加权MAE与MSE对照；
3. **Influence of Sequence Length**：输入长度20–100；
4. **Influence of the Number of Training Samples**：训练样本数10–100；
5. **Amount of Parameters and Training Time**：参数量、CPU训练时间和收敛轮数；
6. **Discussion and Outlook**。

它直接支持损失、历史长度、少样本、参数量和训练时间分析。它预测的是放电电压/EOD，不是下一循环容量，因此只能借鉴实验设计，不能直接照搬指标。

#### 文献7：PatchFormer

原文实验章节为：

1. **4.1 Hyperparameter settings and evaluation metrics**：使用TPE/Optuna优化关键参数，定义MAE、RMSE、$R^2$、AE、RE及平均指标；
2. **4.2 Performance comparison for RUL prediction**；
3. **4.2.1 CALCE I prediction results**；
4. **4.2.2 CALCE II prediction results**；
5. **4.2.3 NASA prediction results**；
6. **4.2.4 TJU prediction results**；
7. **4.3 Ablation experiments of network structure**：分别去除inter-patch和intra-patch attention。

它最值得参考的是“按数据集和不同SP逐层报告主结果＋统一结构消融”。但原文部分协议把测试电池SP之前的数据用于训练；本文若采用完全隔离的held-out-cell协议，不能直接照搬其数值或公平性表述。

#### 文献8：A physics-informed deep learning framework... with feature subset construction

原文实验章节为：

1. **4.1 Experiment settings**：LOO-CV＋64步滑窗、硬件和超参数；
2. **4.2 Evaluation metrics**：MAE、RMSE、$R^2$和Ecycle；
3. **4.3.1 Prediction accuracy analysis**：CALCE和TJU逐电池结果及预测曲线；
4. **4.3.2 Comparative evaluation of STL**：STL、DWT和VMD比较；
5. **4.3.3 Model comparison experiment**：LSTM、Attn-LSTM、Transformer、Informer、TCN及近年方法，并报告100 epochs耗时；
6. **4.3.4 Ablation experiment**：NO STL、NO PINN、NO MSTE、SINGLE、NO CVA、NO RES；
7. **4.4 Feasibility analysis**：讨论BMS可获取输入、离线校准和在线滚动预测。

它直接支持“特征处理比较＋经典/先进基线分层＋物理损失消融＋工程可行性”的设计。其物理证据主要来自损失消融，并没有反演状态的唯一性或state-to-capacity closure分析。

## B.2 文献中实验出现频次

| 实验类型                             | 八篇中的直接先例 | 结论                           |
| ------------------------------------ | ---------------: | ------------------------------ |
| 数据集/协议/指标/实现设置            |              8/8 | 必写                           |
| 与多类基线的主实验                   |              8/8 | 必写                           |
| 明确的模块或损失消融                 |              6/8 | 基本属于算法论文标配           |
| 参数量、训练时间、资源或延迟         |              5/8 | 若摘要声称高效则必写           |
| 历史长度/关键超参数敏感性            |          至少5/8 | 建议写，但正文可精选           |
| 有限训练数据                         |              3/8 | 用于支撑少数据价值             |
| 显式噪声压力测试                     |              2/8 | 有用但不是所有论文都做         |
| 案例或DVA/注意力解释                 |              2/8 | 有直接参考，但形式依模型而定   |
| 与本文完全一致的严格递归状态多步预测 |              0/8 | 本文专属建议，不是照抄文献     |
| state-to-capacity closure            |              0/8 | 本文专属核心证据               |
| Jacobian/Fisher可辨识性              |              0/8 | 高阶可选项，不是投稿标配       |
| 系统化P95/P99失败案例                |              0/8 | 增强可信度的建议，不是文献共性 |

## B.3 后文建议的来源标签

- **文献常见项**：设置、主对比、模块消融、效率、长度敏感性；
- **文献中有少量先例**：噪声、少数据、DVA/案例、物理损失消融；
- **由文献思路改造**：把噪声施加到原始$V$/$I$曲线、把DVA对应改为五状态灵敏度；
- **本文专属项**：严格递归状态多步预测、曲线反演审计、state-to-capacity closure、物理解码捷径消融；
- **高阶可选项**：Jacobian/Fisher可辨识性、profile likelihood和系统失败分位分析。

因此，后文并不是声称“别人把这些全部做过了”，而是先复用文献中已经形成共识的实验骨架，再为本文独有的贡献补最少必要证据。

## B.4 实验章节的核心论证

本文实验部分应围绕下面的一句话展开：

> 在严格跨电池隔离的容量预测任务中，验证“观测反演—状态演化—物理解码”闭环模型能否同时获得有竞争力的单步精度、稳定的递归预测、可审计的物理一致性和较低的在线计算成本；其解释范围限于有效整电池状态，而非唯一辨识的材料级退化参数。

实验需要依次回答六个问题：

1. **评测是否公平？** 测试电池、目标循环和归一化统计量是否与训练严格隔离？
2. **模型是否有效？** 单步和多步容量预测是否优于 Persistence、简单趋势模型及强数据驱动基线？
3. **闭环是否成立？** 反演状态能否重建放电曲线，并通过同一物理解码器恢复容量？
4. **增益来自哪里？** 状态反演、多尺度状态表示、受限转移和物理解码分别贡献了什么？
5. **困难条件下是否可靠？** 噪声、缺测、稀疏采样、先验失配和有限训练数据下表现如何？
6. **实际代价是多少？** 除神经网络外，首次状态反演、缓存、物理解码和端到端推理分别需要多少时间和内存？

## B.5 相关文献的实验组织及可借鉴内容

| 文献                                                                             | 原文实验结构                                                                                | 对本文的直接启发                                                                       |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| *A Brain-Inspired Spiking Network Framework...*                                  | Experimental setup；Eunicell案例；Powerstream案例；resource consumption comparison          | 主结果应覆盖不同数据来源，并把资源消耗作为独立证据，而不是只报告精度。                 |
| *AttMoE*                                                                         | 数据集、基线、指标和参数；总体比较；MoE消融；dropout消融与高斯噪声敏感性；时间成本          | 核心模块消融应与其设计动机对应；噪声鲁棒性和耗时应单独报告。                           |
| *Physics-Informed Neural Networks for Prognostics and Health Management...*      | 多工况案例；SoH估计；RUL预测；显式动力学与DeepHPM比较；自适应损失；网络结构和候选输入敏感性 | 物理模型不仅要与数据模型比较，还要验证物理先验形式、损失平衡和状态辨识稳定性。         |
| *BATTER-MoE*                                                                     | 数据与协议；主比较；组件协同消融；关键超参数；参数/FLOPs/延迟；噪声；有限目标数据迁移       | 一篇完整算法论文通常还需要超参数、效率、噪声和低数据量实验；所有结果报告多次运行。     |
| *BatteryMFormer*                                                                 | 跨域总体结果；不同早期观测长度；组件消融；代表案例；DVA对应分析；50%训练数据实验            | 可以通过DVA/曲线形状把学习表示与电化学敏感区域联系起来；应测试历史长度和数据效率。     |
| *Inherently Interpretable Physics-Informed Neural Network...*                    | 三数据集主实验；损失函数对照；序列长度；训练样本数；参数量和训练时间                        | 解释性模型应展示少样本和长度敏感性，同时比较精度稳定性、参数量与总训练成本。           |
| *PatchFormer*                                                                    | 超参数优化与指标；CALCE I/II、NASA、TJU不同SP主实验；预测曲线；网络结构消融                 | 不同SP和不同数据集应形成一致协议；但本文不能沿用“将测试电池SP之前数据加入训练”的做法。 |
| *A physics-informed deep learning framework... with feature subset construction* | LOO设置；指标；逐电池结果；分解方法比较；经典/先进基线；物理损失和网络消融；效率与可行性    | 物理约束必须设置“去掉物理项”的直接对照；逐电池结果、EOL误差和可部署输入均需说明。      |

上述文献形成了共同的证据阶梯：

$$
\text{协议可信}
\rightarrow
\text{主任务有效}
\rightarrow
\text{组件因果归因}
\rightarrow
\text{困难条件与泛化}
\rightarrow
\text{效率与适用边界}.
$$

本文在这条阶梯之前还应加入“观测—状态—容量闭环验证”，这是区别于一般数据驱动网络的必要证据。

---

# C. 早期压缩版方案（已由A节扩展取代）

本节是上一轮形成的六小节压缩方案，仅保留为决策历史。正式写作和实验执行应以A节的九小节结构为准；若出现冲突，采用A节。

这一部分给出建议直接写入论文的实验章节结构。它不是把前述文献中的所有实验机械叠加，而是以本文实际主张为约束，只保留能够形成完整证据链的内容。整体叙事顺序为：先证明比较协议可信，再证明预测有效，随后解释性能来源并验证物理闭环，最后交代计算代价、鲁棒性和适用边界。

## C.1 早期建议采用的章节目录

### 实验设置

统一说明数据集与化学体系、原始测量、循环构造与预处理、跨电池划分、单步/多步任务、评价指标、对比模型、实现配置和统计方式。这里不讨论结果，也不按CALCE、NASA和TJU分别重复模型流程；三者只在“可用原始测量”和“与化学体系相匹配的固定物理先验”上有所不同，观测反演、状态递推和容量解码的架构与训练目标保持一致。

### 总体容量预测性能

本节以主表报告CALCE、NASA和TJU的十次独立运行结果，并用一张逐测试电池网格图展示真实容量、平均预测、标准差和逐循环误差。图形主要借鉴MSTEA-Net Fig. 3–4；BATTER-MoE Fig. 4仅用于参考误差条和跨数据集统一视觉编码。

本节只能回答“预测是否准确以及是否能跨数据来源复现”，不能仅凭三条视觉上贴合的曲线宣称物理可解释性或跨化学零样本泛化。所有模型应使用相同测试电池、相同历史信息和相同目标循环；主文报告平均结果而非最佳随机种子。

### 递归多步预测

从同一观测窗口出发，在不读取未来真实容量、曲线或反演状态的条件下，比较不同预测跨度的递归误差和EOL偏差。主表报告$H=4,8,16,32$（必要时增加64）的MAE/RMSE与EOL AE/RE，主图同时展示误差—跨度曲线和早、中、晚寿命三个起点的代表性rollout。该实验不是一般文献的固定项目，但如果摘要和方法将“状态演化”作为贡献，它就是验证该贡献所必需的证据。

### 消融与设计分析

用一张分组大表完成核心消融，不为每个小模块单独制造一张表。建议分成四组：

1. **信息路径**：去除曲线反演、用直接容量历史替代状态、取消物理解码；
2. **状态演化**：去除受限转移、去除残差更新、去除快/慢时间尺度中的任一分支；
3. **训练目标**：分别去除曲线重建、容量闭合、状态约束和多步一致性项；
4. **关键设计量**：历史长度、状态维数或容量网格仅选择最关键的一项进入正文，其余放补充材料。

消融结果应同时报告单步MAE、多步MAE、物理闭合误差和参数量，使读者能够区分某个组件究竟提高了预测精度、递归稳定性，还是物理一致性。BATTER-MoE、BatteryMFormer、PatchFormer、MSTEA和AttMoE均提供了“围绕模块设计动机组织消融”的直接先例。

### 物理一致性与状态解释

本节是本文区别于一般数据驱动模型的核心，不宜被拆散到大量附加实验中。正文保留两张证据图和一张紧凑表：

- **观测闭合图**：在早、中、晚寿命阶段展示真实与重建的$V(Q)$，并在下方给出$dV/dQ$或重建残差；
- **状态闭合图**：展示五维有效状态的寿命轨迹、由同一状态解码得到的容量以及状态—容量局部灵敏度；
- **物理一致性表**：报告电压曲线重建误差、截止点误差、state-to-capacity closure误差和状态越界率。

这里的结论应限定为“有效整电池状态具有观测一致性、容量闭合性和局部响应性”。除非另有独立测量，不把隐状态写成被唯一辨识的材料级参数。BatteryMFormer将注意区域与DVA特征对应、MSTEA通过去除物理损失检验物理项，提供了相近的解释实验先例；但曲线反演和state-to-capacity closure是本文模型特有、因此必须补充的证据。

### 效率、鲁棒性与适用性

正文首先报告完整链路的成本：参数量、FLOPs（若可可靠计算）、首次反演时间、缓存后单步延迟、递归每步延迟、峰值内存和训练时间。建议使用“误差—延迟”或“误差—参数量”Pareto图配合效率表，而不是只罗列网络参数量，因为本文的反演和物理解码也会产生实际开销。该设计可直接参考BATTER-MoE的资源受限分析、AttMoE的时间成本以及BattNN的参数量/训练时间比较。

噪声、缺测、降采样和有限训练数据可在本节用一个紧凑压力测试表概括；若摘要不声称鲁棒或少样本能力，则完整曲线放入补充材料。最后给出失败案例和适用边界，包括化学先验失配、曲线覆盖不足、异常循环以及EOL附近的误差放大，避免把“跨三个数据集有效”扩大解释成任意化学体系、任意测量条件下的普适性。

## C.2 主张—证据—载体对应关系

| 论文主张                 | 最低必要证据                                       | 主文载体                       | 不能据此延伸出的结论                       |
| ------------------------ | -------------------------------------------------- | ------------------------------ | ------------------------------------------ |
| 跨电池容量预测准确       | 相同协议下的强基线、逐电池结果、10次运行mean ± std | 主结果表＋逐测试电池容量网格图 | 不等于跨数据集zero-shot泛化                |
| 状态递推适合长期预测     | 无未来观测的严格递归rollout、误差随$H$变化         | 多步表＋horizon/rollout图      | 解析有界不等于长期预测准确                 |
| 物理引导确有作用         | 去除物理路径/损失的消融，且同时观察精度和闭合误差  | 分组消融表                     | 单一物理损失降低误差不等于状态真实可辨识   |
| 反演状态具有可解释性     | $V(Q)$重建、截止点、容量闭合、状态灵敏度           | 物理闭环图＋一致性表           | 不等于材料参数被唯一识别                   |
| 架构可迁移到不同数据来源 | 同一架构和训练目标在CALCE、NASA、TJU复现           | 三数据集主表/主图              | 不等于不需要数据集匹配的测量映射和化学先验 |
| 模型轻量高效             | 完整在线链路的延迟、内存、参数和精度—成本关系      | 效率表＋Pareto图               | 不能只用神经网络参数量代表总成本           |

## C.3 主文图表的早期预算

主文建议控制在六张表、五张实验图以内。每张图只承担一个核心结论，避免同一结果在表格、柱状图和折线图中重复出现。

### 主文表格

1. **表1｜Datasets and evaluation protocols**：化学体系、额定容量、循环数、原始测量、训练/验证/测试电池、SP、历史长度、预测跨度和EOL阈值；
2. **表2｜Compared methods and reproducibility**：方法类别、输入、是否有源码、年份、复现或重训练方式；
3. **表3｜One-step prediction across three datasets**：逐数据集/逐测试电池MAE、RMSE、$R^2$和mean ± std；
4. **表4｜Recursive multi-step prediction**：不同$H$的误差和EOL AE/RE；
5. **表5｜Grouped ablation and physical closure**：完整模型与四组关键变体的预测、闭合和复杂度指标；
6. **表6｜End-to-end efficiency and stress-test summary**：冷启动、在线延迟、内存及少量代表性压力条件。

### 主文图件

1. **图E1｜Three-dataset capacity-prediction overview**：CALCE、NASA、TJU三列；容量真值、十次运行平均预测、标准差带、逐循环绝对误差和EOL局部放大。核心结论仅为“跨数据来源的预测准确性与稳定性”；
2. **图E2｜Recursive forecast degradation with horizon**：误差—跨度曲线＋早/中/晚寿命rollout。核心结论为“递归状态转移是否抑制长期漂移”；
3. **图E3｜Observation-to-state reconstruction**：早/中/晚$V(Q)$、$dV/dQ$或残差及截止点。核心结论为“反演状态能够解释观测曲线”；
4. **图E4｜State-to-capacity closure and sensitivity**：五状态轨迹、解码容量与局部灵敏度。核心结论为“状态不仅相关，而且能通过既定解码路径闭合到容量”；
5. **图E5｜Accuracy–cost trade-off**：MAE—延迟或MAE—参数量Pareto图。核心结论为“精度不是由不可接受的计算开销换取”。

不再建议将噪声、缺测、先验失配、全部超参数、Jacobian奇异值和多个失败案例各自做成主文图，否则会稀释主线。它们作为补充图S1–S5更合适。

## C.4 容量轨迹图的旧版讨论

此前拟采用BATTER-MoE式代表电池综合图；最终方案已改为A节的“全部正式测试电池网格图”，不再重复绘制代表电池综合图。最终图仍须满足：

1. 一张含逐电池数值和运行间方差的主表，避免仅凭代表曲线判断优劣；
2. 固定测试电池和SP，避免事后挑选最好看的轨迹；
3. 相同坐标语义、统一容量单位和清楚标注的EOL阈值；不同额定容量时优先分别保留真实Ah坐标，不为视觉对齐而隐去物理尺度；
4. 与Persistence的数值比较，因为平滑退化曲线即使采用持久性预测也可能在视觉上高度贴合；
5. 十次运行均值与标准差带；逐电池单次最优值只能用于调试，不能代替主结果；
6. 正文报告全部正式测试电池的平均预测轨迹；补充材料提供逐随机种子和逐基线曲线。

## C.5 主文与补充材料的边界

| 放置位置             | 内容                                                                                                          |
| -------------------- | ------------------------------------------------------------------------------------------------------------- |
| 主文必留             | 评测协议、三数据集数值主表与逐测试电池容量网格图、严格多步结果、核心消融、曲线/容量双闭合、完整链路效率       |
| 主文压缩呈现         | 一个代表性鲁棒性表、一个失败案例、最关键的历史长度或状态维数敏感性                                            |
| 补充材料             | 全部逐电池/逐种子曲线、完整超参数搜索、不同噪声/缺测/降采样强度、多初值反演、更多状态轨迹和失败案例           |
| 暂不作为投稿前置条件 | 系统Jacobian/Fisher可辨识性、profile likelihood、zero-shot迁移、P95/P99全分位审计；除非正文明确提出对应强主张 |

## C.6 投稿前的三类审稿风险

1. **协议与统计风险**：TJU当前双侧局部$3\sigma$插值可能读取目标循环之后的邻域信息；正式主结果必须采用不清洗或仅依赖过去信息的因果清洗，并重新运行。滑动窗口高度重叠，统计检验不能把所有窗口当作独立样本，实验单位应是电池或独立训练运行。
2. **物理解释风险**：不同化学体系可以使用匹配的固定OCP/物理先验，但必须明确“统一的是架构与优化目标，不是化学参数本身”。任何没有独立材料测量支撑的隐状态，都只能称为有效状态或代理状态。
3. **公平性与复现风险**：测试电池不能因为文献原协议而被并入训练；基线需区分原文结果与同协议本地复现；超参数选择必须基于训练/验证集，最终测试集不能用于挑配置、挑种子或挑代表图。

---

# D. 详细实验实施库

> 本部分是按模块维护的实验实施库，不代表论文正文的呈现顺序。正式写作和图表编号一律以A节的“4.1设置→4.2单步→4.3多步→4.4反演→4.5消融→4.6物理分析→4.7鲁棒性→4.8效率与边界”为准。

以下内容保留为执行层面的实验清单，不表示所有项目都必须进入主文。实际写作以第3节的最终结构和图表预算为准。

## D.1 实验问题与评测概览

实验章节开头只需用一段话声明评测目标，不重复方法背景。建议将实验组织为以下研究问题：

- **RQ1：预测有效性。** 模型能否在未见电池上准确预测下一循环容量？
- **RQ2：递归稳定性。** 不使用任何未来观测时，状态递推能否在多步预测中抑制漂移？
- **RQ3：物理一致性。** 反演状态是否同时保留曲线形状、截止事件和容量信息？
- **RQ4：设计必要性。** 各反演约束、时间尺度、状态转移形式和物理解码是否不可替代？
- **RQ5：架构适用性与鲁棒性。** 在分别训练且配置已知物理条件时，同一主体架构能否适用于不同数据集，并在噪声、缺测和先验失配下保持可靠？
- **RQ6：效率与边界。** 模型的冷启动和在线成本如何，在哪些条件下会失效？

这一小节不放结果，只说明后续小节如何回答上述问题。

## D.2 实验设置

### D.2.1 数据集与电池级划分

主文建议使用 CALCE、NASA 和 TJU 三个具有循环内电压、电流和时间记录的数据集。三者均进入相同的“曲线反演—状态转移—物理解码”主体流程，但使用各自已知的放电电流、截止电压和化学体系 OCP 先验。

**表1｜数据集、输入与评测划分。**

| 数据集 | 电池及角色                           | 化学体系         | 原始输入                              | 额定容量 | 放电电流/截止电压     |   历史长度 | SP或预测区间 |
| ------ | ------------------------------------ | ---------------- | ------------------------------------- | -------: | --------------------- | ---------: | ------------ |
| CALCE  | CS2系列；逐电池留一测试              | 待按数据说明冻结 | 放电时间、电流、电压、内阻            | 约1.1 Ah | 按原始协议            |     建议64 | `[待冻结]`   |
| NASA   | B0006/B0007/B0018训练验证；B0005测试 | LCO体系代理      | 放电时间、电流、电压；阻抗记录        |   2.0 Ah | 2 A；电池特定截止电压 | 当前任务16 | 50；可补90   |
| TJU    | CY25-2/CY25-3训练验证；CY25-1测试    | NCM/NCA          | `time/s`、`Ecell/V`、`<I>/mA`、循环号 |   2.5 Ah | 2.5 A；2.5 V          |         64 | 200和400     |

需要明确区分三种“泛化”概念：

1. 同一数据集内部的未见电池预测是**跨电池泛化**；
2. 在TJU上沿用同一架构但配置NMC/NCA先验是**架构可迁移性**；
3. 不重新训练、直接把一个数据集的权重用于另一个数据集才是**跨数据集零样本迁移**。

在没有完成第3种实验前，不应将TJU结果写成“zero-shot cross-dataset generalization”。

### D.2.2 原始循环构造与预处理

本小节必须公开可复现的曲线生成规则：

1. 根据循环编号划分循环，并以负电流恒流区间识别主放电段；
2. 对存在两段放电的参考循环，保留从高电压开始的主放电段，排除后续辅助放电；
3. 由时间与电流积分构造容量坐标，而不是直接使用可能累计异常的设备容量列；
4. 截取到数据集规定的首次截止电压，并在相邻采样点之间线性插值；
5. 对电压、时间和容量坐标执行单调性、有限值和长度检查；
6. 训练电池统计量用于状态标准化，验证与测试阶段冻结。

需要设置一个**预处理因果审计**：逐项检查异常值处理、移动平均、标准化、状态缓存和容量轴对齐是否使用目标循环或未来循环信息。

特别注意：BATTER-MoE和MSTEA-Net均报告了局部$3\sigma$异常值删除与插值，但对测试序列使用双侧邻域会引入未来信息。本文建议：

- 主结果使用原始容量或只依赖历史的因果异常检测；
- 若为了复现文献协议使用双侧$3\sigma$，必须作为单独的“literature-compatible preprocessing”结果报告；
- 两种预处理结果不能混在同一主表中。

### D.2.3 预测任务与无泄漏协议

#### 单步任务

$$
\{\mathcal C_{k-L+1},\ldots,\mathcal C_k\}
\rightarrow \widehat Q_{k+1}.
$$

目标循环的曲线、容量和反演状态均不能进入输入。测试电池在训练、验证、早停、超参数选择和标准化统计量估计中完全不可见。

#### 多步任务

$$
Z_{k-L+1:k}
\rightarrow \widehat Z_{k+1}
\rightarrow \cdots
\rightarrow \widehat Z_{k+H}
\rightarrow \widehat Q_{k+1:k+H}.
$$

多步预测必须采用严格递归状态 rollout。第1步之后不得重新读取真实未来曲线、容量或反演状态。若每一步都用真实历史更新窗口，该实验只能称为连续单步预测，不能称为多步预测。

建议预测跨度：

$$
H\in\{4,8,16,32,64\},
$$

并从早期、中期和晚期三个寿命阶段分别启动。NASA寿命较短时可将最长跨度限制为32，但必须让所有对比模型使用相同目标索引。

### D.2.4 对比方法与公平性

基线应覆盖“简单统计—经典深度模型—现代时序模型—电池专用模型—物理引导模型”五层，而不是只选与本文差异很大的模型。

**表2｜建议基线及其作用。**

| 类型         | 建议模型                                           | 主要回答的问题                                 |
| ------------ | -------------------------------------------------- | ---------------------------------------------- |
| 无训练基线   | Persistence、线性趋势外推                          | 容量高度平滑时，复杂模型是否真正超过简单延续？ |
| 统计/浅层    | AR、SVR或GPR                                       | 深度状态转移是否优于低容量模型？               |
| 循环网络     | LSTM、GRU                                          | 多尺度状态表示是否优于传统序列编码？           |
| 卷积模型     | TCN或ModernTCN                                     | 局部/层次卷积是否足够？                        |
| 通用时序模型 | DLinear、TimesNet/TimeMixer、iTransformer          | 与近年强时间序列基线比较。                     |
| 电池专用模型 | PatchFormer、RUL-Mamba、AttMoE（能公平复现者）     | 与近年电池容量/RUL强基线比较。                 |
| 物理引导     | 可运行的PINN/混合模型、本文的physics-loss-only变体 | 显式状态闭环相对“物理只进入损失”的价值。       |

公平性规则：

- 同一数据集内使用相同电池划分、目标循环、SP和预处理；
- 同一任务内为所有模型提供相同长度的历史信息；
- 容量输入模型、完整曲线输入模型和额外健康指标模型应标注输入差异；
- 原文引用结果与本地复现结果分表或显式标记；
- 不允许用测试电池SP之前的数据训练本文模型，却不给其他基线相同信息；
- 超参数只根据训练/验证电池确定，不以测试电池最优单次结果选配置。

### D.2.5 评价指标与统计报告

容量预测报告：

$$
\mathrm{MAE},\quad \mathrm{RMSE},\quad R^2,
$$

EOL/RUL报告：

$$
\mathrm{AE}=|T_{\mathrm{EOL}}-\widehat T_{\mathrm{EOL}}|,
\qquad
\mathrm{RE}=\frac{\mathrm{AE}}{T_{\mathrm{EOL}}-T_{\mathrm{SP}}}.
$$

还应报告：

- Persistence MAE及相对改善率；
- 早期、中期、晚期分段MAE；
- 多步误差随$H$增长的曲线；
- P95绝对误差和最差电池结果；
- 10个随机种子的mean ± std，主文不以单次最好值替代均值；
- 每块电池结果和跨电池宏平均。

滑动窗口高度重叠，不能把所有窗口当作独立样本直接进行普通t检验。若进行显著性分析，应以电池或连续循环块为重采样单元，采用配对区块bootstrap或按电池配对的非参数检验。

### D.2.6 实现细节

正文精选报告：优化器、学习率、batch size、hidden dimension、层数、历史长度、早停、随机种子数、容量网格范围/点数、OCP先验、截止电压、电流和主要损失权重。完整配置放入补充材料或代码仓库。

硬件部分报告CPU、GPU、内存、PyTorch和SciPy版本，并说明反演在CPU还是GPU完成。

---

## D.3 观测—状态反演有效性

该小节应位于预测主结果之前。其任务不是证明预测网络优于基线，而是证明输入给状态转移器的五维状态是可用且闭合的。

### D.3.1 原始曲线与反演审计

每个数据集报告：

- 原始循环数、成功解析数、进入反演的循环数；
- 缺失/异常/多段放电循环数；
- 反演成功率和多初值优化失败率；
- 参数边界命中率；
- 每循环反演耗时。

TJU需要额外报告参考循环的第二段放电排除数量，以证明设备容量累计异常没有进入容量轴。

### D.3.2 电压曲线重建

选取每块电池的早期、中期和晚期代表循环，展示真实与重建的：

- $V(Q)$曲线；
- $dV/dQ$曲线；
- 截止点附近局部放大图；
- 电压残差随容量的位置分布。

统计量至少包括全曲线电压MAE/RMSE、$dV/dQ$误差和截止点局部误差。只展示几条视觉上吻合的曲线不足以证明总体一致性。

### D.3.3 状态—容量闭合

定义

$$
\widehat Q_k^{\mathrm{cl}}
=G_{\mathrm{phys}}(Z_k),
\qquad
\epsilon_k^{\mathrm{cl}}
=|\widehat Q_k^{\mathrm{cl}}-Q_k|.
$$

**表3｜各数据集的反演与闭合结果。**

| 数据集/电池 | 曲线数 | 反演成功率 | 电压MAE | $dV/dQ$误差 | closure MAE | closure P95 | 边界命中率 |
| ----------- | -----: | ---------: | ------: | ----------: | ----------: | ----------: | ---------: |
| CALCE       |        |            |         |             |             |             |            |
| NASA        |        |            |         |             |             |             |            |
| TJU         |        |            |         |             |             |             |            |

允许形成的结论是“反演状态与所用观察方程及容量解码器内部一致”；不能据此声称五个状态等于真实材料参数或具有唯一解。

---

## D.4 跨电池单步容量预测

这是第一项主实验。每个数据集以未见电池为测试对象，比较完整寿命预测区间内的下一循环绝对容量。

**表4｜跨电池单步容量预测。**

| Model                  | Input        | Params | CALCE MAE/RMSE/$R^2$ | NASA MAE/RMSE/$R^2$ | TJU MAE/RMSE/$R^2$ |
| ---------------------- | ------------ | -----: | -------------------- | ------------------- | ------------------ |
| Persistence            | 最后容量     |      0 |                      |                     |                    |
| Linear trend           | 容量历史     |        |                      |                     |                    |
| LSTM/GRU               | 统一历史输入 |        |                      |                     |                    |
| ModernTCN              | 统一历史输入 |        |                      |                     |                    |
| iTransformer/TimeMixer | 统一历史输入 |        |                      |                     |                    |
| PatchFormer/RUL-Mamba  | 按公平适配   |        |                      |                     |                    |
| MGI-DSSM               | 反演状态     |        |                      |                     |                    |

主表之外应提供：

- 每块测试电池的真实—预测容量曲线；
- 逐循环绝对误差；
- 与Persistence的逐循环误差差值；
- 早、中、晚寿命分段结果；
- 10次运行误差条。

### D.4.1 逐测试电池真实容量—预测容量对比图

直接参考MSTEA-Net Fig. 3和Fig. 4，为全部正式测试电池建立一张多面板图。每个电池的上面板展示真实容量、十次运行平均预测和标准差带，下面板展示相对Persistence的逐循环误差差；同时标出SP和EOL阈值。

MSTEA-Net原文Fig. 3覆盖CALCE #35–#38，Fig. 4覆盖TJU #1–#3，并使用黑色、红色和绿色分别表示真实容量、预测容量和预测误差。本文沿用其逐电池完整轨迹的证据逻辑，但统一使用本文配色并增加跨种子标准差，以适配十次独立运行的统计协议。

不再额外制作“三数据集代表电池综合图”。逐电池图已经覆盖跨数据集轨迹，重复图不会提供新的证据；模型排名由数值表完成。

---

## D.5 不使用未来观测的递归多步预测

这是第二项主实验，也是证明状态空间建模价值的关键。完整模型和所有基线从相同历史窗口出发，递归预测$H$步。

**表5｜不同预测跨度下的递归预测结果。**

| Model                | $H=4$ | $H=8$ | $H=16$ | $H=32$ | $H=64$ | EOL AE/RE |
| -------------------- | ----: | ----: | -----: | -----: | -----: | --------: |
| Persistence          |       |       |        |        |        |           |
| Linear extrapolation |       |       |        |        |        |           |
| GRU/LSTM rollout     |       |       |        |        |        |           |
| 强时序基线           |       |       |        |        |        |           |
| MGI-DSSM             |       |       |        |        |        |           |

应绘制：

1. MAE/RMSE随预测跨度增长的曲线；
2. 早期、中期和晚期起点的典型rollout轨迹；
3. 五维状态随递归步数的漂移、越界率和截止容量；
4. 最坏起点的失败案例。

多步实验需要同时回答精度和稳定性。状态始终为正或更新幅度有界，只能证明解析约束成立，不能替代长期误差结果。

---

## D.6 补充实验候选：有限数据与跨数据集权重迁移

本节不属于当前正文的必要证据链。TJU已经提供完整放电曲线，能够进入与CALCE/NASA相同的主体流程；三数据集分别训练的结果应并入D.3和D.4，用于验证**架构适用性**。只有当摘要或贡献明确提出“小样本学习”或“跨数据集权重迁移”时，才执行并升级以下实验。

### D.6.1 有限源数据

借鉴BATTER-MoE和BatteryMFormer，使用训练电池数据的25%、50%、75%和100%，测试电池保持完全不变。比较完整模型与数据驱动基线的精度退化速度。

由于每个数据集电池数量很少，应优先减少训练电池的可用循环块，而不是随机删除滑动窗口；随机删窗口会保留大量相邻重复信息，夸大“少数据”能力。

### D.6.2 可选零样本迁移

只有在论文需要声称权重可迁移时才进行：源数据集训练后，不在目标数据集更新神经网络参数，直接测试目标电池。随后再增加少量目标训练电池做适配，形成zero-shot、few-shot和full-training三组。

若未完成该实验，应删除“跨数据集零样本泛化”相关表述。

与化学体系匹配/失配的OCP profile对照不放在本节，而归入正文D.9的物理先验鲁棒性实验；该对照检验的是先验敏感性，不是权重泛化。

---

## D.7 消融实验

消融应围绕贡献组织，不建议把所有因素做成不可解释的巨大笛卡尔积。正文放一张核心消融表，补充材料再放模块内部敏感性。

### D.7.1 核心贡献消融

**表6｜核心设计消融。**

| Variant                           | 反演状态 | 多尺度历史 | 受限双向转移 | 物理解码 | 单步MAE | 32步MAE | closure MAE | 曲线MAE |
| --------------------------------- | -------: | ---------: | -----------: | -------: | ------: | ------: | ----------: | ------: |
| Full model                        |        ✓ |          ✓ |            ✓ |        ✓ |         |         |             |         |
| Capacity-only GRU                 |        — |          ✓ |            ✓ |   学习头 |         |         |           — |       — |
| Current state only                |        ✓ |          — |            ✓ |        ✓ |         |         |             |         |
| Monotone transition               |        ✓ |          ✓ |         单向 |        ✓ |         |         |             |         |
| Unconstrained additive transition |        ✓ |          ✓ |            — |        ✓ |         |         |             |         |
| Learned absolute-capacity head    |        ✓ |          ✓ |            ✓ |        — |         |         |           — |         |
| Residual head $Q_k+\Delta Q$      |        ✓ |          ✓ |            ✓ |        — |         |         |           — |         |

物理解码消融必须同时包含学习型绝对容量头、残差头和Persistence，以检验模型是否通过上一循环容量捷径获得低误差。

### D.7.2 状态反演消融

建议依次删除或替换：

- $dV/dQ$形状残差；
- 截止点强调；
- 内阻弱观测；
- 相邻循环连续性；
- 多初值策略；
- 鲁棒least-squares；
- 电流积分容量轴，改为设备`Q discharge`直接值；
- 化学体系匹配OCP；
- 容量自适应状态边界。

评价不能只有预测MAE，还应包括电压重建、closure、边界命中率、多初值方差和反演失败率。

### D.7.3 多尺度时序表示消融

比较：

- 只使用状态水平；
- 状态水平＋一阶差分；
- 去掉8步趋势；
- 去掉32步趋势；
- 同时去掉两种趋势；
- GRU替换为MLP；
- 历史长度16、32、64、128。

这一组应重点观察32/64步rollout，而非只看一步MAE，因为长期趋势分支的价值可能主要体现在递归稳定性。

### D.7.4 学习目标消融

比较完整目标与：

- 仅容量损失；
- 去掉状态监督；
- 去掉曲线监督；
- 去掉晚寿命权重；
- 加入/取消方向损失；
- 固定损失权重与自适应平衡。

实验前必须进行代码级梯度审计，确认每个损失对可训练参数产生非零梯度。没有实际参与优化的损失不得作为贡献或消融项。

### D.7.5 交互消融

只对具有明确耦合关系的模块做交互消融：

- 无多尺度＋学习型容量头；
- 单调转移＋物理解码；
- 无曲线监督＋无状态监督；
- OCP失配＋固定小容量边界。

用于判断两个模块是互补、冗余还是一方只在另一方存在时有效。

---

## D.8 物理一致性与可解释性分析

### D.8.1 五维状态轨迹

展示$Q_{\mathrm{Li}}$、$C_n$、$C_p$、$R_0$和$R_p$随循环的轨迹，并标记：容量恢复、平台、快速衰减、参数边界和EOL。报告跨随机初值的轨迹均值与方差，而不是只展示一条平滑曲线。

### D.8.2 状态—容量灵敏度

计算无量纲局部灵敏度

$$
\widetilde S_i(Z)
=\frac{z_i}{G_{\mathrm{phys}}(Z)}
\frac{\partial G_{\mathrm{phys}}(Z)}{\partial z_i},
$$

并用中心有限差分复核自动微分。分别比较早、中、晚寿命阶段，以回答不同状态在何时影响截止容量。

### D.8.3 局部可辨识性

对观察算子计算Jacobian奇异值谱和近似Fisher信息矩阵条件数，并结合：

- 多初值反演；
- 电压/电流噪声扰动；
- OCP先验扰动；
- 参数相关矩阵；
- 边界命中率。

如果多个状态高度相关或某维长期低灵敏，应将其解释为组合有效状态。该结果用于限定解释边界，而不是强行证明唯一可辨识。

### D.8.4 局部回升与可回调状态转移

预先定义$\Delta Q_k>0$或容量平台片段，比较双向受限转移、强单调转移和无约束转移。分别报告恢复片段和普通片段的MAE、状态方向命中率及多步漂移。

这里的“reversible”仅表示有效状态允许局部回调，不能写成电化学老化机理可逆。

### D.8.5 失败案例

统一选择：绝对误差P95/P99循环、closure误差最大循环、状态边界命中循环、多步漂移最快起点和截止阈值附近平台。每个案例同时展示原始曲线、反演状态、预测容量和基线结果。

---

## D.9 鲁棒性与压力测试

借鉴AttMoE和BATTER-MoE的噪声实验，但将扰动施加在本文真实输入上。

**表7｜建议的困难条件。**

| 扰动           | 建议强度               | 评价目的                     |
| -------------- | ---------------------- | ---------------------------- |
| 电压高斯噪声   | 按设备量级及1×/2×/4×   | 曲线反演和状态稳定性         |
| 电流噪声/偏置  | 小幅随机噪声与系统偏置 | 容量积分误差传播             |
| 采样点随机缺失 | 10%、30%、50%          | 稀疏BMS记录适用性            |
| 循环缺失       | 连续缺失1、4、8个循环  | 状态转移对不规则观测的鲁棒性 |
| 曲线降采样     | 原始、1/2、1/4、1/8    | 曲线分辨率需求               |
| 截止电压扰动   | 协议值附近小扰动       | 截止事件敏感性               |
| OCP先验失配    | 匹配/轻微扰动/错误体系 | 物理先验依赖性               |

每种扰动同时报告曲线重建、closure、单步和多步误差，以区分“反演受损”与“状态转移受损”。

---

## D.10 超参数与协议敏感性

该小节用于说明结论不是来自单一巧合配置，不应继续使用测试电池搜索最优超参数。

建议分析：

- 历史长度$L\in\{16,32,64,128\}$；
- 短/长趋势窗口组合；
- GRU隐藏维度；
- 状态相对更新幅度$\alpha$；
- 极化时间常数$\tau_p$；
- 容量网格点数及网格上限；
- 状态、曲线和容量损失权重；
- 不同SP和不同寿命阶段。

超参数曲线使用训练/验证结果选择配置，测试集仅用于最终一次冻结评测。正文保留最关键的2–3项，其余放入补充材料。

---

## D.11 计算效率与部署成本

文献通常只报告神经网络参数量和推理时间，但本文包含离线曲线反演，因此必须区分冷启动与缓存后在线成本。

**表8｜端到端效率。**

| Model        | NN Params | FLOPs | 训练时间 | 单循环反演 | 全数据反演 | 缓存大小 | 状态转移推理 | 物理解码 | 在线端到端延迟 | 峰值内存 |
| ------------ | --------: | ----: | -------: | ---------: | ---------: | -------: | -----------: | -------: | -------------: | -------: |
| 数据驱动基线 |           |       |          |          — |          — |        — |              |        — |                |          |
| MGI-DSSM     |           |       |          |            |            |          |              |          |                |          |

应分别在CPU和GPU上、batch size = 1下测量推理延迟，并进行预热。反演、状态转移和物理解码分别重复足够次数，报告中位数和P95。

如果神经转移器很小但首次反演较慢，准确表述应为“compact online state-transition model with offline inversion”，不能笼统声称整个流程始终比数据驱动模型更快。

---

## D.12 证据总结与讨论部分衔接

实验章节最后用一段话总结已得到直接证据的结论：预测性能、递归稳定性、物理闭合、关键模块贡献、鲁棒性和成本。对于未被实验直接证明的材料级机理、零样本迁移和长期安全保证，不在结果段延伸解释，而是转入Discussion说明边界。

---

# E. 详细实验库的完整表图清单

### 主文表格

1. 表1：数据集、原始输入、化学体系和跨电池划分；
2. 表2：基线类型、输入和复现来源；
3. 表3：单步主实验；
4. 表4：多步主实验；
5. 表5：曲线反演与物理闭合；
6. 表6：核心消融；
7. 表7：鲁棒性；
8. 表8：端到端效率。

### 主文图件

1. 图1：统一评测和无泄漏数据流示意；
2. 图2：参考MSTEA-Net Fig. 3–4的逐测试电池容量网格图，展示真实容量、十次运行平均预测、标准差带和相对Persistence的逐循环误差差；
3. 图3：误差随多步horizon增长；
4. 图4：早、中、晚期$V(Q)$与$dV/dQ$重建；
5. 图5：五维状态轨迹、EOL和恢复片段；
6. 图6：状态—容量灵敏度及Jacobian奇异值；
7. 图7：噪声、缺测与先验失配压力测试；
8. 图8：代表性失败案例。

### 补充材料

- 正文逐电池平均轨迹对应的完整逐种子、逐基线结果；
- 全部超参数敏感性；
- 详细反演边界与多初值稳定性；
- 更多曲线重建和失败案例；
- 原文结果与本地复现结果对照；
- 数据索引、目标循环索引和配置文件。

# F. 实验执行优先级

### 第一优先级：投稿最低证据闭环

1. 冻结无泄漏数据划分和目标索引；
2. Persistence、线性趋势及6–10个强基线；
3. 单步跨电池主实验，10个随机种子；
4. 严格递归的4/8/16/32步预测；
5. 电压曲线重建和state-to-capacity closure；
6. 物理解码、多尺度表示和转移形式三类核心消融；
7. 参数量、冷启动反演和缓存后推理时间。

### 第二优先级：显著增强可信度

1. NCM/NCA与LCO先验匹配/失配实验；
2. 噪声、缺测、降采样压力测试；
3. 五状态轨迹、灵敏度和恢复片段；
4. 历史长度、趋势窗口和容量网格敏感性；
5. 失败案例和P95/P99误差。

### 第三优先级：有余力再加入

1. 跨数据集zero-shot/few-shot迁移；
2. 有限训练数据实验；
3. Jacobian/Fisher信息的系统可辨识性分析；
4. bootstrap/profile likelihood状态不确定性；
5. 温度或变工况数据集上的扩展实验。

# G. 实验开始前必须冻结的事项

1. CALCE、NASA和TJU各自的历史长度、SP、多步horizon和EOL阈值；
2. 主结果采用原始/因果清洗还是文献兼容的双侧$3\sigma$清洗；
3. 每个数据集的训练、验证和测试电池，以及固定目标循环索引；
4. 哪些基线能获得源码并完成同协议复现；
5. OCP先验的来源、化学体系和是否在测试电池上校准；
6. 曲线监督是否对模型参数产生有效梯度；
7. 单步最好值仅用于调试，论文主结果统一使用预注册配置的mean ± std；
8. “解释性”统一限定为有效状态的观察一致性、容量闭合和局部灵敏度，不写成材料参数唯一辨识。

# H. 当前结果使用前的审计提醒

当前TJU训练已经证明原始曲线能够进入同一模型结构，并取得很低的初步误差；但该配置启用了双侧局部$3\sigma$插值。正式论文结果应在因果清洗或不清洗条件下重新运行，并把文献兼容预处理作为单独对照。完成这一审计之前，不应直接把当前数值放入主结果表。
